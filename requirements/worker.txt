# requirements/worker.txt
# Production dependencies for the Celery worker image.
# Intentionally excludes fastapi/uvicorn — workers don't serve HTTP.

-r base.txt

# ML inference
torch==2.3.1
torchvision==0.18.1
timm==1.0.3

# Task queue & cache
celery[redis]==5.4.0
redis==5.0.7

# Observability
prometheus-client==0.20.0
boto3==1.34.144